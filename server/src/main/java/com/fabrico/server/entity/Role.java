package com.fabrico.server.entity;

/**
 * User Role Enum
 * 
 * Defines different access levels in the system
 * - USER: Regular customers (can browse, buy products)
 * - ADMIN: System administrators (can manage products, orders, users)
 */
public enum Role {
    USER,
    ADMIN
}
